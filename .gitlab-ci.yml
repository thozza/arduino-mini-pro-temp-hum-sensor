build:
        script:
                - virtualenv -p python venv
                - source venv/bin/activate
                - pip install platformio
                - prep.sh
                - platformio run
                - rm -rf .pioenvs
